该版本完善内容如下：
1、将HashMap<String,int[]>改为HashMap<String,String>；
2、行列宽高值转换；
3、读入方式除路径外，增加String类型读入方式；
4、将Parser中的静态成员变量去掉（存在一个小问题在后面）。

问题：（一些小想法）
目前ArrayList<Configuration>不能再全局任何地方调用，我想设置一个ArrayList<Configuration>作为全局变量存储当前输入的配置信息，
 	①应该怎么做？将Configuration设为静态类？新建一个类专门用于存储当前配置信息？
	②接①新建专用存储类，有没有同时输入多个配置信息的情况？
	怎么区分不同的输入信息，以“其路径+文件名”为名字（对于数据库我想知道如何读入，读入方法需要我提供么？）？还是我依次进行编号？还是在Configuration内再增加一个字段，用于表示第几个配置信息？